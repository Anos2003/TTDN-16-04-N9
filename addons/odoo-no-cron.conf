[options]
# Database settings
db_host = db
db_port = 5432
db_user = odoo
db_password = odoo
db_name = danganh1009

# DISABLE CRON WORKERS HOÀN TOÀN - FIX CONNECTION ERROR
max_cron_threads = 0

# Connection pool settings
db_maxconn = 64

# Server settings
addons_path = /mnt/extra-addons,/usr/lib/python3/dist-packages/odoo/addons

# Workers (single worker để tránh connection issues)
workers = 0

# Timeout settings
limit_time_cpu = 600
limit_time_real = 1200

# Log settings
log_level = warn
logfile = False
